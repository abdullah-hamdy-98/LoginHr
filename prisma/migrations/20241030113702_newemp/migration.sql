BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[EmpFiles] ALTER COLUMN [EmpCode] NVARCHAR(15) NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
